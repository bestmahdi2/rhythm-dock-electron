# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Dependency directories
# This folder contains all of your project's dependencies.
# It can be very large and is generated by running `npm install`.
/node_modules

# Build output folder
# The 'dist' folder is created by electron-builder when you package your app.
/dist
/release

# Logs
# These files are often generated for debugging purposes.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
# These are system files that don't belong in the repository.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor directories and files
# Prevents sharing personal editor settings with other collaborators.
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Your project includes an .npmrc file to set an Electron mirror.
# It's good practice to ignore this file to prevent accidentally committing
# sensitive auth tokens, even though yours is currently safe.
.npmrc

# Environment variables
.env

# Dependencies
/node_modules

# Temporary directories from user
/old/

# Build files
/dist/
/build/